/**
 * Login Page
 *
 * TODO:
 * - Email + password form
 * - Call POST /api/auth/login on submit
 * - Store JWT token (localStorage or httpOnly cookie)
 * - Redirect to / on success
 * - Show error message on failure
 * - Link to /register for new users
 *
 * Hints:
 * - Use the useAuth() hook (from hooks/useAuth.ts)
 * - Use React state for form fields and error messages
 * - Disable submit button while loading
 */

export default function LoginPage() {
  // TODO: Implement login form
  return (
    <main>
      <h1>Login</h1>
      <form>
        {/* Email input */}
        {/* Password input */}
        {/* Submit button */}
        {/* Link to register */}
      </form>
    </main>
  );
}
